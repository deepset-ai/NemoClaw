"""Generated custom components.

The meta-agent's `create_component` tool writes new `@component` classes into this
package. Modules here are referenced from serialized configs by import path and are
covered by the deserialization allowlist (see `security_agent.optimize.validate.ALLOWLIST_PATTERNS`).

Generated code is executed only inside the benchmark subprocess and, when HITL mode
is on, only after human approval of the `create_component` call.
"""
